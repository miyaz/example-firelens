server {
    listen       80;

    set_real_ip_from ${VPC_CIDR};
    real_ip_header X-Forwarded-For;

    # goに振り分け
    location / {
        expires -1;
        proxy_pass http://${GOAPP_HOST}:9000;
    }
}
